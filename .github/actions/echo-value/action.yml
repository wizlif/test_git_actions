name: "Echo value"
inputs:
  COOL_BOOLEAN:
    description: "Boolean value to echo"
    type: boolean
    default: false
runs:
  using: "composite"
  steps:
    - name: ⚙️ Echo
      shell: bash
      run: |
        echo "COOL_BOOLEAN: ${{ inputs.COOL_BOOLEAN }}"
